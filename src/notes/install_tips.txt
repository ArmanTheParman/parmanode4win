pip3 install colorama
pip3 install requests
pip3 install psutil
pip3 install winshell (for installation)
pip3 install pywin32 (for installation)

DONT Install python3 from windows store
Ensure not a windows store version of python. Do "where python" to see if 'Microsoft' in the path.

To make an execuatble, need pyinstaller:
pip install -U pyinstaller
